spring:
  application:
    name: book-backend

  datasource:
    url: jdbc:mysql://localhost:3306/bookwalk
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: aivle

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect


